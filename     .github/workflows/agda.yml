name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: wenkokke/setup-agda@v2
        with:
          agda-stdlib-version: 'recommended'

      - run: agda CategoryAction/Prelude.agda
        working-directory: src
